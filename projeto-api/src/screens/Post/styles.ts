import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
  },

  label: {
    fontSize: 16,
    fontWeight: "bold",
    marginBottom: 6,
  },

  input: {
    borderWidth: 1,
    borderColor: "#CCC",
    padding: 8,
    borderRadius: 6,
    marginBottom: 12,
    fontSize: 16,
  },

  textArea: {
    height: 120,
    textAlignVertical: "top",
  },
});
